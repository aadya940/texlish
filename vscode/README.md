# Texlish VSCode extension

Support for [the Texlish language](https://texlish.org) in `.texlish` files.

## Features

* Syntax highlighting
* Automatic indentation after `: environment` and `\command:`
